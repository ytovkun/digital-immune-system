# Цифрова імунна система — залежності
# Встановлення:  pip install -r requirements.txt
# Python 3.9+

# GenAI-ядро (генерація атак + ШІ-аналітик захисту)
anthropic==0.105.2

# HTTP: red team agent, експлойти, форвардинг проксі
requests==2.32.5

# Inline reverse-proxy цифрової імунної системи
flask==3.1.3

# Production WSGI-сервер для проксі (замість Flask dev-сервера)
waitress==3.0.2

# Автоматичні unit-тести (dev)
pytest==8.4.2

# Візуальний дашборд результатів (таблиці, графіки, ROC) — streamlit run dashboard.py
streamlit==1.40.2
pandas-stubs==2.2.2.240807
altair==5.5.0

# Решта — стандартна бібліотека Python (json, sqlite3, threading,
# collections, hashlib, re, pathlib) — встановлення не потрібне.
